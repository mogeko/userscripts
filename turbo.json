{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["../../release/**"]
    },
    "lint": { "outputs": [] },
    "fmt": { "outputs": [], "cache": false }
  }
}
