{
  "name": "template",
  "version": "0.0.1",
  "private": true,
  "description": "There is a template to create UserScript.",
  "scripts": {
    "build": "tsc && rollup --config rollup.config.js",
    "lint": "prettier --check .",
    "fmt": "prettier --write ."
  },
  "keywords": [
    "userscript",
    "tampermonkey"
  ],
  "author": "Mogeko",
  "license": "MIT",
  "devDependencies": {
    "tsconfig": "workspace:*"
  }
}
